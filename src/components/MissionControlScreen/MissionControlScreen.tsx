/**
 * MissionControlScreen
 */

import * as React from 'react';

// eslint-disable-next-line
import styles from './MissionControlScreen.styles';

export interface Props {}

const MissionControlScreen: React.FC = (props: Props) => {
  return (
    <div>
      <h1>MissionControlScreen</h1>
    </div>
  );
};

export default MissionControlScreen;
