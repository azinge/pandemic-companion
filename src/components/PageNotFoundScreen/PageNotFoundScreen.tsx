/**
 * PageNotFoundScreen
 */

import * as React from 'react';

// eslint-disable-next-line
import styles from './PageNotFoundScreen.styles';

export interface Props {}

const PageNotFoundScreen: React.FC = (props: Props) => {
  return (
    <div>
      <h1>PageNotFoundScreen</h1>
    </div>
  );
};

export default PageNotFoundScreen;
